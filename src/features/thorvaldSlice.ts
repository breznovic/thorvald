import { createSlice } from "@reduxjs/toolkit";

const initialState: {} = {};

export const thorvaldSlice = createSlice({
  name: "thorvald",
  initialState,
  reducers: {},
});

export const {} = thorvaldSlice.actions;

export default thorvaldSlice.reducer;
